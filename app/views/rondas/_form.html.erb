<%= form_for(@ronda) do |f| %>
  <% if @ronda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ronda.errors.count, "error") %> evitó que se guardara la ronda:</h2>

      <ul>
      <% @ronda.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div data-role="fieldcontain" >
    <fieldset data-role="controlgroup" data-type="horizontal">
    	<legend >Fecha</legend>
    	<%= f.date_select :fecha %>
    </fieldset>
  </div>
  <div class="actions">
    <%= f.submit "Aceptar" %>
  </div>
<% end %>
