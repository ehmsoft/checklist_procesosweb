<h1>Lista de Rondas</h1>

<table>
  <tr>
    <th>Fecha</th>
    <th>Usuario</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rondas.each do |ronda| %>
  <tr>
    <td><%= ronda.fecha %></td>
    <td><%= ronda.usuario.email %></td>
    <td><%= link_to 'Ver', ronda %></td>
    <td><%= link_to 'Editar', edit_ronda_path(ronda) %></td>
    <td><%= link_to 'Eliminar', ronda, method: :delete, data: { confirm: '¿Seguro que desea borrar la ronda?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva Ronda', new_ronda_path %>
