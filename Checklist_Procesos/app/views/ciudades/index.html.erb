<h1>Lista de Ciudades</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Departamento</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ciudades.each do |ciudad| %>
  <tr>
    <td><%= ciudad.nombre %></td>
    <td><%= ciudad.departamento %></td>
    <td><%= link_to 'Ver', ciudad %></td>
    <td><%= link_to 'Editar', edit_ciudad_path(ciudad) %></td>
    <td><%= link_to 'Eliminar', ciudad, method: :delete, data: { confirm: '¿Segurito segurito?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Crear Ciudad', new_ciudad_path %>
